# Dependências
node_modules/

# Variáveis de ambiente (nunca comitar segredos)
.env
**/.env
!**/.env.example

# Vercel CLI (link local do projeto — específico de cada máquina)
.vercel

# Prisma client gerado
**/node_modules/.prisma/

# Logs
*.log
npm-debug.log*

# SO / editor
.DS_Store
Thumbs.db
.thumbnail

# Repo é PRIVADO — os materiais do cliente (uploads/CNPJ.pdf, imagens da marca)
# são intencionalmente versionados. Não adicionar aqui.
